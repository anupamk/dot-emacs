# ------------------------------------------------------------------------------
# ignore auto-generated files
anupam-emacs-module/*
anupam-lisp/*
early-init.el
init.el

# ------------------------------------------------------------------------------
# ignore backup, autosave, byte-compiled files
.#*
*.el~
*.*~
*.elc
*.eld
.*

# ------------------------------------------------------------------------------
# yes, this is intended. `emacs-init.el' is generated from emacs-init.org
emacs-init.el
emacs-init.html

# ------------------------------------------------------------------------------
# org-roam.db is also ignored because actual 'knowledge-base' is
# stored in the 'org-roam-directory'
org-roam.db

# ------------------------------------------------------------------------------
# ignore elpa/* this is fetched everytime we initialize the configuration
elpa/*

# ------------------------------------------------------------------------------
# and other transient stuff that we can ignore
transient/*
tramp
save-hist
save-place
save-point
prescient-items
recentf
package-quickstart.el
custom.el
desktops/*
local/*
bookmarks
elpy/*
ac-comphist.dat
projectile-bookmarks.eld
projectile.cache
places
projects
url/*
eln-cache/*
history
save-point-place
*custom.el
session*

# ------------------------------------------------------------------------------
# for 'straight' we don't care about either the 'repos' or the 'build'
# sub-directories.
#
# the 'version' sub-directory however contains pinned versions (see
# 'straight/versions/default.el') of various packages that we use, and
# for config reproduceability etc. we want that to be checked-in with
# other user configuration.
straight/repos/*
straight/build/*
straight/build-cache.el
